{
	"version": "0.1.0",
	
	// The command is tsc. Assumes that tsc has been installed using npm install -g typescript
	"command": "xcopy",
	
	// The command is a shell script
	"isShellCommand": true,

	// Show the output window only if unrecognized errors occur. silent
	"tasks": [{
		"taskName": "CopyFiles",
		"isBuildCommand": true,
	 "showOutput": "never",
	"suppressTaskName": true,
	"args": ["${workspaceRoot}\\wp-content","C:\\wamp\\www\\partnered\\wp-content", "/H","/R", "/E","/Y"]
	}]
}